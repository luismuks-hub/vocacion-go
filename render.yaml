services:
  - type: web
    name: vocacion-go
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python servidor.py
    envVars:
      - key: RENDER
        value: true
    plan: free
